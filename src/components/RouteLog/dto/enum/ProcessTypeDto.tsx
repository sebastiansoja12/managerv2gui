enum ProcessTypeDto {
    CREATED = "CREATED",
    RETURN = "RETURN",
    ROUTE = "ROUTE",
    REJECT = "REJECT",
    REROUTE = "REROUTE",
    REDIRECT = "REDIRECT",
}

export default ProcessTypeDto;
