enum ParcelStatusDto {
    CREATED = "CREATED",
    RETURN = "RETURN",
    ROUTE = "ROUTE",
    REJECT = "REJECT",
    REROUTE = "REROUTE",
    REDIRECT = "REDIRECT",
    SENT = "SENT",
    DELIVERY = "DELIVERY",
}

export default ParcelStatusDto;
